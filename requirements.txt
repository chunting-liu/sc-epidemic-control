# Versions the records in results/ were produced with.
# NumPy 2.x is required: the control code uses numpy.trapezoid,
# which replaced numpy.trapz in NumPy 2.0.
numpy==2.2.6
scipy==1.15.3
networkx==3.4.2
matplotlib==3.10.9
